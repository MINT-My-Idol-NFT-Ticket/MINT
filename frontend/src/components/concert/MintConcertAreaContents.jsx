import React from 'react'
import { Box, Typography } from '@mui/material'

function MintConcertAreaContents(props) {
  return (
    // <Box>
    //   <Typography sx={{ position: 'sticky', top: '0', backgroundColor: '#cacaca' }}>지정석</Typography>
    //   {sections.map((section, idx) => (
    //     <MintConcertAreaList
    //       key={section.name}
    //       section={section.name}
    //       leftover={section.seats}
    //       idx={idx}
    //       pick={pickTime}
    //       selected={isSelected && idx === selectedId}
    //     />
    //   ))}
    // </Box>
    <></>
  )
}

export default MintConcertAreaContents
